import Defi from "@/component/defi";
import Wrapper from "@/layouts/Wrapper";

export const metadata = {
	title: "De-Fi",
};

const index = () => {
	return (
		<Wrapper>
			<Defi />
		</Wrapper>
	);
};

export default index;
